# .............................................................................
##   Columns
##   1) key: required, original name or zero-based column index,
##   2) shortname: required, <= 10 chars,
##   3) type: required, integer, string, or real, 
##   4) role: optional for TaxaName, UniqueID field,
##            required for Longitude, Latitude, Geopoint, GroupBy fields,
##   4or5..n) optional, all additional values are acceptedvalues , 
# .............................................................................
# Note, This file must have the same basename as the CSV file it describes
# 
# '#' indicates a comment line, all others must contain comma-separated values:
#  IFF a field performs a specific role in these data, role name should be the 
#      4th value on a line
#   1st column:  Key = original field name (matching the header in data file) 
#                     or column index (Zero-based)
#   2nd column:  Shortname = a new field name, 10 chars or less
#   3rd column:  Type = integer, string, or real
#   4th column:  Role = if applicable for this field, Longitude, Latitude, Geopoint, 
#                                              GroupBy, TaxaName, UniqueID
#   All following columns (starting at 4 or 5):  accepted values for this field 
#
# Role (case-insensitive) MUST be present 2-4 of the fields
#    Required:
#       GroupBy field contains value to group records by. 
#    Required: Either Longitude & Latitude or Geopoint
#       Longitude field contains x coordinate
#       Latitude field contains y coordinate
#       Geopoint field contains both x and y coordinates
#           CSV data will be in the format {"lat": -16.35, "lon": -67.616667}
#    Optional:
#       UniqueID field contains a unique ID for each record. Values will be 
#          generated if missing. 
#    Optional:
#       TaxaName field contains the taxa name for the recordset. If records
#          for a dataset have different values, the first record will
#          determine the dataset displayname.  Records will be   
#          named with GroupBy field if this role is undefined.
#
# .............................................................................

  # Field names
  gbifid, gbifid, integer, UniqueID
  datasetkey, datasetkey, string 
  occurrenceid, occurid, string
  kingdom, kingdom, string
  phylum, phylum, string
  class, class, string
  order, order, string
  family, family, string
  genus, genus, string
  species, species, string, TaxaName
  infraspecificepithet, isepithet, string 
  taxonrank, taxonrank, string, SUBSPECIES, SPECIES
  scientificname, sciname, string
  countrycode, cntrycode, string
  locality, locality, string
  publishingorgkey, puborgkey, string
  decimallatitude, dec_lat, real, Latitude
  decimallongitude, dec_long, real, Longitude
  elevation, elevation, real
  elevationaccuracy, elev_acc, real
  depth, depth, real
  depthaccuracy, depth_acc, real
  eventdate, eventdate, string
  day, day, integer
  month, month, integer
  year, year, integer
  taxonkey, taxonkey, integer, GroupBy
  specieskey, specieskey, integer
  basisofrecord, basisofrec, string
  institutioncode, inst_code, string
  collectioncode, coll_code, string
  catalognumber, catnum, string
  recordnumber, recnum, string
  identifiedby, idby, string
  rights, rights, string
  rightsholder, rightshold, string
  recordedby, rec_by, string
  typestatus, typestatus, string
  establishmentmeans, estabmeans, string
  lastinterpreted, lastinterp, string
  mediatype, mediatype, string
  issue, issue, string

